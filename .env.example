# Optional disposable local-beta overrides.
# Repository does not load this file automatically.
# Keep state inside ignored local-state/ and use loopback ports only.
LNSAT_LOCAL_BETA_STATE_DIR=./local-state/beta
LNSAT_LOCAL_BETA_PORT=55432
